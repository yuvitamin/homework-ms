spring:
  application:
    name: sentinel-test
  cloud:
    nacos:
      config:
        #  配置中心的地址
        server-addr: 39.108.64.63:8848
        #  配置文件的后缀名
        file-extension: yml
        refreshable-dataids: false
